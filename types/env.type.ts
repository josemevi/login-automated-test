//model for the variables inside the .env that will be use into the test configuration
export type EnvConfigType = {
    mainUrl: string,
    cookieConsent: string,
    cookieConsentValue: string,
    cookieDomain: string
}